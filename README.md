# AES-E COMMERCE

Essay scoring experiment for e-commerce related writing data.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
python essay_scoring.py
```

Check model paths, input data paths, and output directories in the script before running on a new machine.
